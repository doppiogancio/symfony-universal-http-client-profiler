# symfony-universal-http-client-profiler
A Symfony bundle that profiles all outgoing HTTP requests — in Web, CLI, Messenger workers, and background processes. Includes a dedicated Web Profiler panel, request timeline, headers/body inspection, stack traces, and full CLI session archiving.
